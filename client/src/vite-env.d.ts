/// <reference types="vite/client" />

interface ImportMetaEnv {
  /** Optional support email shown on Request access / Forgot password (e.g. it@company.com). */
  readonly VITE_CONTACT_EMAIL?: string;
}
